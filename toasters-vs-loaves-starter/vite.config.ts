import { defineConfig } from "vite";
// Set base to your repo name for GitHub Pages if using a project page,
// e.g. base: "/toasters-vs-loaves/". For a user/org page, leave as "/".
export default defineConfig({
  base: "/toasters-vs-loaves/",
});
